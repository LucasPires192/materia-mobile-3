import { Text, Image, ScrollView, StyleSheet } from 'react-native';

const Home = () => {
    return (
        <ScrollView>
            <Text>Home</Text>
        </ScrollView>
    )
}

export default Home;