import { Text, Image, ScrollView, StyleSheet } from 'react-native';

const About = () => {
    return (
        <ScrollView>
            <Text>About</Text>
        </ScrollView>
    )
}

export default About;