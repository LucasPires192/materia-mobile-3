import { Text, Image, ScrollView, StyleSheet } from 'react-native';

const Contact = () => {
    return (
        <ScrollView>
            <Text>Contact</Text>
        </ScrollView>
    )
}

export default Contact;